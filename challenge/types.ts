
export interface Game {
  gameTitle: string; 
  description: string; 
  gameId: string;
}

export interface Event {
  eventTitle: string;
  imagem: string;
  imagemEvent: string;
  eventAuthor: string;
  eventData: string;
  eventText: string;
  eventId: string;
}
